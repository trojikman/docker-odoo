version: '3.4'
services:
  web:
    tty: true
    build: .
    depends_on:
      - db
      - wdb
    ports:
      - "8069:8069"
      # Этот порт нужен для лонгполлинга
      - "8072:8072"
    volumes:
      - odoo-web-data:/var/lib/odoo
      - ./config:/etc/odoo
      - ../odoo12/pos-addons:/mnt/addons/pos-addons
    environment:
      - WDB_SOCKET_SERVER=wdb
      - WDB_NO_BROWSER_AUTO_OPEN=True
      - ODOO_QUEUE_JOB_HOST=saas.127.0.0.1.nip.io
    # TODO: unify parameter passing for different cases to avoid making different branches
    # '--db-filter=^%d$$' в этой записи если не экранировать $, то вызовет ошибку
    command: ['-d', 'pos', '--init', 'pos_multi_session', '--db-filter=.*', '--workers=2']

  db:
    image: postgres:10
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_USER=odoo
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - odoo-db-data:/var/lib/postgresql/data/pgdata
  wdb:
    image: kozea/wdb:3.3.0
    ports: ['1984:1984']

volumes:
  odoo-web-data:
  odoo-db-data:
